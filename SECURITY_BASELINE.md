# SKILL 安全与隐私基线

## 适用范围

本基线适用于 `xiaozhi-skills` 仓库中的所有 `SKILL.md`、`references/` 与 JSON Schema。

目标：
- 降低 ClawHub / ReviewClawScan 对默认激活、长期记忆、跨 SKILL 共享、定时提醒、自动化触发的安全告警概率
- 统一所有 SKILL 的授权边界、共享边界和用户控制入口

本基线是**规则来源**；具体词表、授权位、降级路径与共享片段的**唯一定义**在 `shared/`：

| 文件 | 与本基线的关系 |
|---|---|
| `shared/vocab.md` §8 | 授权位的字段定义与语义 |
| `shared/platform-conventions.md` 三、四、五 | 自动措辞禁令、控制入口标准段落、提醒入队契约 |
| `shared/crisis-exception.md` | 危机例外片段，优先级高于本基线的一切低敏与授权规则 |
| `shared/grade-bands.md` | 学段参数（免打扰窗口、作息、课时） |

违反项由 `npm run check` 拦截，不依赖人工复核。

## 一、默认原则

所有 SKILL 都应遵守以下默认原则：

1. 先回答当前问题，再决定是否需要长期记忆或跨 SKILL 联动。
2. 未经用户明确同意，不建立跨会话档案，不发送提醒，不共享给其他 SKILL。
3. 只读取当前任务所需的最小必要字段，不读取完整历史档案。
4. 用户始终拥有查看、更正、删除、暂停、取消共享、导出的控制权。
5. 提醒只有一个发送方（`xiaozhi-im-reminder`）；其他 SKILL 只能入队，不得自行承诺"我会在某时提醒你"。
6. 危机信号优先于一切：出现自伤、轻生、遭受伤害、持续严重绝望、家庭安全问题时，立即停止当前流程，按 `shared/crisis-exception.md` 处置。

## 二、推荐文案

### 1. 涉及长期记忆

推荐写法：

```text
仅在用户明确同意后建立或读取长期档案。
未获同意时，只使用当前会话信息，不创建跨会话记录。
```

避免写法：

```text
必须激活此SKILL
凡是涉及…务必调用此SKILL
每次对话开始前静默检查
自动存入DNA
永久记得你
```

### 2. 涉及提醒或调度

推荐写法：

```text
仅在用户明确要求设置提醒或同意接收该类提醒时启用。
用户可随时暂停、恢复、取消、调整频率。
连续三条无回应即暂停该类提醒，下次进入会话时再询问是否恢复。
```

避免写法：

```text
主动提醒
自动设置提醒
超过几天无对话自动唤醒
每周一次直到恢复使用
默认配置提醒
```

### 3. 涉及跨 SKILL 共享

推荐写法：

```text
仅向声明过用途的 SKILL 共享完成当前任务所需的最小字段摘要。
不传整份档案，不默认写回长期存储。
```

避免写法：

```text
任何SKILL运行前优先读取DNA
拉取其他所有活跃SKILL
写入成长轨迹
默认同步到IM提醒
```

### 4. 涉及台账与状态写入（老师端）

推荐写法：

```text
生成待确认条目，由老师确认后写入课时台账 / 作业状态 / 弱项标签。
```

避免写法：

```text
自动预扣课时
24 小时未处理自动确认
自动补排
自动写回学生档案
```

## 三、必须包含的控制入口

### 3.1 六项控制口令

凡是涉及长期记忆、提醒、档案、履历、DNA、报告汇总的 SKILL，文案中应提供至少一组可执行控制：

- 查看：`查看我的档案` / `查看我的提醒`
- 更正：`更正我的档案`
- 删除：`删除我的档案`
- 暂停：`这次不要记忆` / `暂停提醒`
- 共享控制：`不要共享给其他SKILL` / `不要给家长看`
- 导出：`导出我的档案`（以文本形式给出，便于转存）

统一写法见 `shared/platform-conventions.md` 第四节；`scripts/check-skills.mjs`（检查项 P1）会校验每个持有数据的 SKILL 是否包含"查看我的…"与"删除我的…"口令。

### 3.2 授权主体与学段

- 长期档案、情绪记录、家长可见输出的授权位定义见 `shared/vocab.md §8`，字段落在 `dna-profile.schema.json#/meta/consentStatus`。
- 授权必须记录**主体**：`consentGivenBy` 取 `学生本人` / `监护人` / `学生与监护人`。
- 小学各学段（约 14 周岁以下）必须由监护人同意，`consentGivenBy` 必须包含"监护人"；学段由 `ageBand` 记录，`guardianConsentRequired` 由学段推导。
- 学生与监护人共用一个 IM 会话时，先确认说话人；**确认之前进入受限模式**：不读长期档案、不写任何记录、不执行删除、不变更授权位、不输出家长版内容，只用当前会话信息作答。不得用“默认按学生本人处理”绕过确认。
- 家长可见内容双门控：学习摘要需 `parentSharingConsent`，含情绪内容再需 `emotionSharingWithParent`（且由学生本人同意）。
- 老师端写回学生档案需 `teacherWritebackConsent`，并只能走交接协议的 `teacher_writeback`。
- 危机例外（`shared/crisis-exception.md`）优先于以上所有授权位。

## 四、最小化记录原则

默认不记录以下信息：

- 真实姓名、真实住址、精确定位、身份证件、联系电话与其他联系方式
- 出生年月等可精确定位到个人的字段（如确需年龄，只记学段）
- 财务、医疗、家庭纠纷等与学习任务无直接关系的信息
- 与当前学习任务无关的长篇闲聊细节
- 危机事件的具体细节与原话（只记"已转介"的处置事实）

若用户主动提及，也应优先改写为低敏概括，并如实告知该项不会写入。学员一律使用化名或座号。

## 五、字段级 Schema 防护（写入约束）

凡是通过 JSON Schema 写入长期工作空间、共享档案或可导出结构（`dna-profile`、`handover-protocol`、`solo-teacher-workspace`、`class-teaching-workspace`）的字段，Schema 本身必须承担第一道防线，而不能只依赖大模型层守门：

- **长度硬约束**：所有可能承载人话摘要的 `string` 字段（特别是 `parentSummary`、`factSummary`、`description`、`notes`、`usageNotes` 等），必须设置合理的 `maxLength`，推荐不超过 500 字符。超长输入一律在生成阶段收敛，不允许在存储阶段截断。
- **枚举/格式约束**：业务可枚举的状态、星期、等级、类型等字段必须使用 `enum` / `format`，避免自由文本写入。联系方式类字段一律用枚举表达"偏好"，不接受自由文本。
- **description 写明禁止项**：高敏字段必须在 `description` 中显式声明禁止写入的内容类型（真实姓名、身份证件、联系方式、账户信息、医疗诊断、心理标签、家庭纠纷、财务细节等），让模型在生成时就能看到约束。
- **授权位硬校验**：涉及情绪写入与教师写回的交接，由 Schema 的条件校验强制要求对应授权位为真，而不是只在正文里写一句提醒。
- **大模型层与 Schema 层双重把关**：SKILL 的"边界"章节中必须复述上述禁止项；Schema 校验作为最后一道兜底。模型层先做低敏改写，Schema 层再做结构和长度校验。
- **草稿态不得放宽**：`draft` / `sentStatus: draft` 等中间态不放松字段级防护，字段级约束适用于任何写入。
- **错误处理优先级**：当用户主动提供高敏信息时，模型应先提示"该项不会写入工作空间"，再询问是否改写为可记录的版本，不得静默丢弃也不得静默写入。

新增或修改 JSON Schema 时，应在 PR 自检中确认以上各点，并运行 `npm run check:schemas`。

## 六、关键结论写入门槛

以下内容写入长期档案前，至少满足一项：

- 用户明确确认
- 连续多次对话或练习结果支持（口径见 `shared/vocab.md §5`）
- 小测、复述、费曼测试等方式验证通过

不要把一次情绪、随口评价或未经验证的推测直接固化为长期标签。写入时必须带置信度（`shared/vocab.md §7`）：样本不足的结论只在当前会话使用，不入档。

老师端由规则命中的弱项（如同类错因累计达阈值）先写入"进步证据"，经老师确认后才写入学员卡的长期弱项字段。

## 七、命名与措辞建议

敏感词并非绝对禁止，但应谨慎使用。若出现下列词语，建议同时补充授权与边界说明：

- `DNA`
- `档案`
- `长期记忆`
- `持久记忆`
- `跨次追踪`
- `提醒`
- `调度`
- `自动化`

如非必要，优先使用以下更稳妥的表达：

- `按需提示` 替代 `主动推送`
- `在用户同意后提醒` 替代 `主动提醒`
- `建议触发` 替代 `自动触发`
- `生成待确认条目` 替代 `自动写入`
- `摘要字段` 替代 `完整档案`

同时避免以下三类表述：

- **承诺类**：`预期提分 X 分`、`治愈焦虑`、`这种感觉就会消失`
- **固定型标签**：`思维天花板`、`有没有天赋`、把一次表现写成长期属性
- **伪精确**：无出处的比例与倍数（`覆盖 80% 场景`、`记得深十倍`、`85% 的学生`）；经验参数须标注为经验值并允许用户调整

## 八、仓库维护要求

新增或修改 SKILL 时，按以下顺序自检，然后运行 `npm run check`：

1. 是否存在"默认激活""默认读取档案""默认共享"的描述
2. 是否存在"自动提醒""自动唤醒""自动写回""自动预扣""自动确认"的描述
3. 是否补充了查看/更正/删除/暂停/取消共享/导出六项入口
4. 是否把长期记忆限制为最小必要字段
5. 是否避免把"第三次出错"之类规则直接写成永久标签
6. 写入工作空间的字段是否在 JSON Schema 中设置了 `maxLength` / `enum` / `format` 等硬约束，并在 `description` 中显式声明高敏禁止项
7. 相关 SKILL 的"边界"章节是否复述了 Schema 的字段级高敏禁止项
8. 术语、阈值、状态词是否直接引用 `shared/vocab.md`，而不是在正文里另立一套
9. 是否声明了 `metadata.grade_bands`，时间与时长类参数是否引用 `shared/grade-bands.md`
10. 会读到情绪文本的 SKILL 是否引入了 `shared/crisis-exception.md` 片段
11. 会生成题目的 SKILL 是否引用了 `shared/ai-item-check.md`
12. 有"不给答案"类铁律的 SKILL 是否改用 `shared/hint-ladder.md` 的提示阶梯并声明默认上限
13. 接口章节中的字段路径是否都真实存在于四份 Schema 之一

## 九、说明

本基线的目的不是削弱 SKILL 能力，而是把能力放在清晰、可控、可解释的边界内。
